# ============ JARVIS requirements ============
# Windows 11 · Python 3.12. Derived mechanically from the imports in jarvis/
# (AST scan → pip distribution names), not maintained by hand — so a fresh
# install actually works. Re-derive after adding a dependency.

# --- Core / brain ---
python-dotenv>=1.0
google-genai>=1.0          # Gemini brain (NOT the deprecated google-generativeai)

# --- Web server + HUD ---
fastapi>=0.110
starlette>=0.37          # imported directly (server.py); ships with fastapi but pin it
uvicorn[standard]>=0.29

# --- Windows integration (REQUIRED — not optional) ---
pywin32>=306               # DPAPI encryption, Recycle Bin, shortcuts, win32gui
pywinauto>=0.6             # UI Automation tree (perception)
comtypes>=1.2              # COM plumbing for pycaw
pycaw>=20230407            # system volume / mute

# --- Input synthesis + screen ---
pyautogui>=0.9.54
pydirectinput>=1.0         # works in fullscreen games
mss>=9.0                   # fast screenshots
Pillow>=10.0
numpy>=2.0
psutil>=5.9
screen_brightness_control>=0.22
pyperclip>=1.8

# --- Voice ---
SpeechRecognition>=3.10    # capture + Google Web Speech STT
PyAudio>=0.2.14            # raw mic capture (prebuilt wheels for cp312)
openwakeword>=0.6          # "hey jarvis" wake word — local, no key
edge-tts>=6.1              # free neural TTS (default voice out)
pyttsx3>=2.90              # offline fallback TTS
pygame>=2.5                # audio playback
faster-whisper>=1.0        # optional local Whisper STT (large download)
elevenlabs>=1.0            # optional premium TTS (needs a key)

# --- Web automation + search ---
playwright>=1.50           # then run once: playwright install chromium
ddgs>=9.0                  # keyless DuckDuckGo search

# --- Email (send_email) ---
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# --- Memory: local semantic retrieval (no torch, no network, no key) ---
# One-time model download: python -m jarvis.core.embedder --setup
onnxruntime>=1.24
tokenizers>=0.20

# --- Tray ---
pystray>=0.19
